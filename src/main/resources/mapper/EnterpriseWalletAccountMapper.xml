<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.korant.youya.workplace.mapper.EnterpriseWalletAccountMapper">

    <!--  根据企业id查询钱包id  -->
    <select id="queryWalletIdByEnterpriseId" parameterType="java.lang.Long" resultType="java.lang.Long">
        SELECT id
        FROM enterprise_wallet_account
        WHERE enterprise_id = #{enterpriseId}
          AND is_delete = 0
    </select>
</mapper>
