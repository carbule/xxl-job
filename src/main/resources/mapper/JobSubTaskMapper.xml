<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.korant.youya.workplace.mapper.JobSubTaskMapper">

    <!--  批量插入子任务  -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO job_sub_task(id, main_task_id, order_id, description, status,
        create_time,is_delete)
        VALUES
        <foreach collection="list" item="param" index="index" separator=",">
            (
            #{param.id},
            #{param.mainTaskId},
            #{param.orderId},
            #{param.description},
            #{param.status},
            CURRENT_TIMESTAMP,
            0
            )
        </foreach>
    </insert>
</mapper>
