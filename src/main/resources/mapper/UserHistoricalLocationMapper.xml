<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.korant.youya.workplace.mapper.UserHistoricalLocationMapper">

    <select id="queryLatestLocation" parameterType="java.lang.Long"
            resultType="com.korant.youya.workplace.pojo.vo.userhistoricallocation.UserHistoricalLocationVo">
        SELECT id,
               city_code AS cityCode,
               city_name AS cityName
        FROM yy_user_historical_location
        WHERE uid = #{userId}
        ORDER BY update_time DESC LIMIT 1
    </select>

    <!--  查询用户历史定位信息  -->
    <select id="queryHistoricalpositioning" parameterType="java.lang.Long"
            resultType="com.korant.youya.workplace.pojo.vo.userhistoricallocation.UserHistoricalLocationVo">
        SELECT id,
               city_code AS cityCode,
               city_name AS cityName
        FROM yy_user_historical_location
        WHERE uid = #{userId}
        ORDER BY update_time DESC LIMIT 3
    </select>
</mapper>
